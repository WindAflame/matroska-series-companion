[project]
name = "matroska-series-companion"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "WindAflame", email = "dev.windaflame@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
matroska-series-companion = "matroska_series_companion:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
